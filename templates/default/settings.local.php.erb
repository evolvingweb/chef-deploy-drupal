<?php
/* local.settings.php, to be included from settings.php
** Generated by Chef for <%= node[:fqdn]%>
*/
<% if node['deploy-drupal']['version'] =~ /^7/ %>
$databases = array (
  'default' =>
  array (
    'default' =>
    array (
      'database' => '<%= @db_name %>',
      'username' => '<%= @db_user %>',
      'password' => '<%= @db_pass %>',
      'host' => 'localhost',
      'port' => '',
      'driver' => 'mysql',
      'prefix' => '',
    ),
  ),
);
<% else %>
  $db_url = "mysql://<%=@db_user%>:<%=@db_pass%>@localhost/<%=@db_name%>;"
<% end %>
?>
<%= IO.read(@custom_file) if !@custom_file.nil? and File.exists?(@custom_file) %>
<?php
