; configuration for APC
; Generated by Chef for <%= node[:fqdn]%>
; on <%= Time.now.asctime %>

extension=apc.so
<% node['deploy-drupal']['apc_directives'].each do |k, v| -%>
<%= "apc.#{k}=#{v}" %>
<% end -%>
